import { rollDie } from "./tasks-math-random/rollDie.js";
import { pickCard } from "./tasks-math-random/pickCard.js";
import { generatePassword } from "./tasks-math-random/passwGenerator.js";
import { generateLotteryNumbers } from "./tasks-math-random/lotteryNumbers.js";

/* 
// Roll random dice number
console.log(rollDie()); // Should output a number between 1 and 6
console.log(rollDie());
console.log(rollDie());
console.log('');

// Pick some random card
console.log(pickCard());
console.log(pickCard());
console.log(pickCard());
console.log('');

// Test the function
console.log(generatePassword(8, false));  // 8-character password without special chars
console.log(generatePassword(12, true));  // 12-character password with special chars
console.log(generatePassword(16, true));  // 16-character password with special chars
 */

/* 
// Test the function
console.log(generateLotteryNumbers(6, 49));  // 6 unique numbers between 1 and 49
console.log(generateLotteryNumbers(5, 35));  // 5 unique numbers between 1 and 35
 */

// TODO: Implement the generateLotteryNumbers function
console.log(generateLotteryNumbers(6, 49));  // 6 unique numbers between 1 and 49
console.log(generateLotteryNumbers(5, 35));  // 5 unique numbers between 1 and 35
