/* Unary */

// let a = Number(prompt('First number?', 1));
// let b = Number(prompt('First number?', 2));
let a = +prompt('First number?', 1);
let b = +prompt('First number?', 2);
// let b = prompt('Second number?', 2);

alert(a + b); // 12
// alert(a + b); // 12

/* using Number() or prepending them with + 
  (hasilnya sama aja)
  */

/* Increment/Decrement */

// prefix
/* let counter = 1;
let a = ++counter; // (*)

alert(a); // 2 */

/* ----------------------------------------------------- */

// postfix
/* let counter = 1;
let a = counter++; // (*) changed ++counter to counter++

alert(a); // 1 */
